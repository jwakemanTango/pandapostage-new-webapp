// ------------------------------------------------------------------
// Unified USB Scale exports (Vanilla + React)
// ------------------------------------------------------------------

// Core (vanilla JS / TS singleton)
export * from "./scale";

// React context wrapper (optional for React apps)
export * from "./scale-react";
